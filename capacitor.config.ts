import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'component-icon-defaults',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
